protocol Hotel {
    init(roomCount: Int)
}

class HotelAlfa: Hotel {
    var roomCount: Int

    required init(roomCount: Int) {
        self.roomCount = roomCount
    }
}

let hotelAlfa = HotelAlfa(roomCount: 50)

print("Количество комнат в отеле Alfa: \(hotelAlfa.roomCount)")
